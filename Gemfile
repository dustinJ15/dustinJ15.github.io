source "https://rubygems.org"

# Jekyll 4 — works with modern Ruby locally. GitHub Pages' classic build pins
# Jekyll 3.9 via the github-pages gem; if the deployed output ever diverges from
# local preview, deploy with a GitHub Actions workflow instead of the classic
# build so the versions match.
gem "jekyll", "~> 4.3"
gem "jekyll-sitemap"
gem "webrick"
